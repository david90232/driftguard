{
  "name": "driftguard",
  "version": "0.2.4",
  "description": "Local-first security drift scanner for repos and AI agent skills with trusted baselines, file hashing, dependency drift, install-hook detection, and risky capability diffs",
  "type": "commonjs",
  "license": "MIT",
  "private": true,
  "bin": {
    "driftguard": "./src/cli.js"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "scan": "node ./src/cli.js scan",
    "scan:fixtures": "node ./scripts/run-fixtures.js",
    "test": "node --test"
  }
}
